#include<stdio.h>

char message[] = "abcdefghijklmnopqrstuvwxyz";

int main(){

 scanf("%s", message);
    printf("Message: %s\n", message);
    
    int i = 0;
    while(message[i] != '\0')
        i++;
    char encrypted[i] ;
    
    int x;
    for(x=0; x!=\0; x++){
        if(index ascii is between 97-122)
        
int i;
char x = 'A';
    scanf("%c", x);
    i = x;
    printf("%c %d", x, i);
    
return 0;
}
