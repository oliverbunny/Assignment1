#include<stdio.h>

char message[] = "abcdefghijklmnopqrstuvwxyz";  //The message to be encrypted

int main(){

    scanf("%s", message);   
    printf("Message: %s\n", message);
    
int i = 0;                                  //This block of code counts the
    while(message[i] != '\0'){                  //length of the "message" array to
        i++;                                    //give the "encrypted" the same length. 
char encrypted[i] ;
    }

int x;
    for(x=0; message[x] != '\0'; x++){          //For loop is to execute encryption algorithms
int j;
    while( j<n ){
        printf("%c = %d\n", str[j], str[j]);
        j++;
    }                                        //for the length of the "message" array only.
        if(index ascii value is between 97-122)
    //Convert from lower case char to upper case      
        else if(index ascii value is between 65-90)
    //Encrypt char by reducing its ascii value by certain amount
        else if()   
    
    
    
    
    
    
/* Code for print ASCII values of Index's from string
  char str[] = "abcdefghijklmnopqrstuvwxyz";
 
    
    scanf("%s", str);

int j;
    for(j=0; str[j] != '\0'; j++){
        printf("%c = %d\n", str[j], str[j]);
    }
    */
return 0;
}
